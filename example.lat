class A {
  int x;
  string s;

  int m(int y) {
    x = y;
    n();
    return y;
  }

  int n() {
    return self.x + 1;
  }
}

class B extends A {
  // int n() {
  //   return self.x - 1;
  // }
}

int main() {
  A a = new B;
  printInt(a.m(2137));
  printInt(a.n());
  return 0;
}