/* parity of positive integers by loop */

int main () {
  string s1 = readString();
  string s2 = readString();
  string s = s1 + s2;
  if (s1 == s2)
    printString(s);
  else
    printString(s1);
  return 0;
}
